import {createClient} from '@/lib/supabase/server';
export default async function MyPage(){const sb=await createClient();const {data:{user}}=await sb.auth.getUser();if(!user)return <div className="shell"><main className="main"><section className="hero"><h1>로그인이 필요해요</h1><p>의뢰와 찜한 공방을 한곳에서 관리할 수 있어요.</p><a className="photo-cta block-button" href="/auth">로그인 / 가입</a></section></main></div>;const [{data:profile},{data:requests}]=await Promise.all([sb.from('yj_profiles').select('*').eq('id',user.id).maybeSingle(),sb.from('yj_requests').select('*').eq('consumer_id',user.id).order('created_at',{ascending:false})]);return <div className="shell"><header className="topbar"><a className="brand" href="/">🏠 옆집공방</a><b>MY</b></header><main className="main"><section className="hero"><h1>{profile?.nickname||'회원'}님 👋</h1><p>{profile?.role==='artisan'?'공방 전문가 계정':'내 의뢰와 진행상황을 확인하세요.'}</p>{profile?.role==='artisan'&&<a className="photo-cta block-button" href="/artisan">장인센터 열기</a>}</section><section className="section"><h2>내 의뢰</h2><div className="list-stack">{requests?.length?requests.map(r=><a className="card row-card" href={`/orders/${r.id}`} key={r.id}><div><span className="star-badge">{r.status}</span><h3>{r.title}</h3><div className="meta">{r.job_type} · {r.handoff_method}</div></div><b>›</b></a>):<div className="empty">아직 의뢰가 없어요. 사진 한 장으로 시작해보세요.</div>}</div></section></main></div>}
