'use client';import {FormEvent,useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Setup(){const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const sb=createClient(),{data:{user}}=await sb.auth.getUser();if(!user)return location.href='/auth';const f=new FormData(e.currentTarget);await sb.from('yj_profiles').upsert({id:user.id,nickname:String(f.get('owner')),role:'artisan'});const {error}=await sb.from('yj_workshops').insert({owner_id:user.id,name:String(f.get('name')),description:String(f.get('description')),address:String(f.get('address')),categories:String(f.get('categories')).split(',').map(x=>x.trim()).filter(Boolean),round_trip_delivery_enabled:f.get('delivery')==='on'});if(error)return setMsg(error.message);location.href='/artisan'}return <div className="shell"><header className="topbar"><a className="brand" href="/artisan">← 장인센터</a><b>공방 등록</b></header><main className="main" style={{maxWidth:600}}><section className="section"><h1>내 공방 열기 🛠️</h1><form className="form-card" onSubmit={submit}><label>대표자/활동명<input name="owner" required/></label><label>공방 이름<input name="name" required/></label><label>소개<textarea name="description" required/></label><label>분야<input name="categories" placeholder="가죽, 수선, 맞춤제작" required/></label><label>주소<input name="address" required/></label><label className="radio"><input name="delivery" type="checkbox"/> 🔄 공방 왕복배달 가능</label><button className="photo-cta">공방 등록하기</button>{msg&&<p className="meta">{msg}</p>}</form></section></main></div>}
